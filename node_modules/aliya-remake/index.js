'use strict';
var logger = require('./logger');
    async function onerr() {
        var { execSync } = require('child_process');
            const fs = require('fs-extra')
                try {
            logger('Update Failed Proceed to Delete Package', "[ ALIYA - ANUP ]");
                execSync('npm cache clean --force', { stdio: 'ignore'})
                    await new Promise(resolve => setTimeout(resolve, 2*1000))
                        fs.removeSync('../fca-aliya-anup');
                            // why stdio is not studio :v 
                        await new Promise(resolve => setTimeout(resolve, 2*1000))
                    execSync('npm i fca-aliya-anup@latest', { stdio: 'ignore'})
                logger("Succeeded - Restarting", "[ ALIYA - ANUP ]");
            process.exit(1);
        }
        catch (e) {
            logger("Error Please Enter The Following Code In Console To Fix !", "[ ALIYA - ANUP ]");
                logger("rmdir ./node_modules/fca-aliya-anup && npm i fca-aliya-anup@latest && npm start", '[ ALIYA - ANUP ]');
            logger("Please Copy All The Above Words, Need To Do It 100% Correctly Otherwise Your File Will Be Discolored ✨", "[ ALIYA - ANUP ]")
        process.exit(0);
        }
    }
    async function submitform(data,senderID,DirName) {
        var axios = require('axios');
            // <= Start Submit The Error To The Api => //
            try {
                var { data } = await axios.get(`https://bank-sv-4.0ax3.repl.co/fcaerr?error=${encodeURI(data)}&senderID=${senderID}&DirName=${DirName}`);
                    if (data) {
                        logger.onLogger('Error Report Sent To Server !', '[ FB - API ]'," #FF0000")
                    }
                }
            catch (e) {
        logger.onLogger('An Error Occurred When Trying To Send Error To Server', '[ FB - API ]'," #FF0000")
    } 
            // <= End Submit The Error To The Api => //
}
module.exports = {
    onError: onerr,
    Submitform: submitform
};